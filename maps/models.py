from django.db import models
"""
class Event(models.Model):
    event_title = models.CharField(max_length=255)
    event_date = models.DateTimeField(auto_now_add=False)
    longitute = models.DecimalField(max_digits=)
"""